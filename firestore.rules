
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Izinkan akses baca dan tulis hanya untuk pengguna yang sudah login
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
